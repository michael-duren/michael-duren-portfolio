import { ControlsContainer } from './controls.component.styles';

const Controls = () => {
  return (
    <ControlsContainer>
      <p>Play/Pause</p>
    </ControlsContainer>
  );
};

export default Controls;
